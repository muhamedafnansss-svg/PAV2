# VAL v15.0 — Unified Dependencies
# ===================================
# STEP 1 — Install PyTorch (choose your build):
#
#   GPU (CUDA 12.1):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   GPU (CUDA 12.4+):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
#   CPU only (slow inference, working):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# STEP 2 — Install all other deps:
#     pip install -r requirements.txt

# ─── Core Runtime ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
psutil>=5.9.0

# ─── ML Framework ─────────────────────────────────────────────────────────────
# torch must be installed separately (see STEP 1)
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.1.99
tokenizers>=0.15.0

# ─── Quantization (GPU — enables 4-bit NF4 for Qwen/Mistral) ─────────────────
bitsandbytes>=0.43.0

# ─── API Server ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ─── Voice Pipeline (JARVIS Mode) ────────────────────────────────────────────
# STT — Faster-Whisper (CTranslate2, 4-6x faster than OpenAI Whisper)
faster-whisper>=1.0.0
# TTS — Piper (CPU, fast, high quality male voice)
# Install separately: pip install piper-tts
# piper-tts>=1.2.0
# Speaker verification (voiceprint auth)
resemblyzer>=0.1.3
# Audio I/O
sounddevice>=0.4.6
soundfile>=0.12.1

# ─── Voice — Fallback (lighter deps) ─────────────────────────────────────────
# openai-whisper>=20231117        # Alternative STT
pyttsx3>=2.90                     # Fallback TTS (system voice)

# ─── OSINT (optional, improves results) ───────────────────────────────────────
python-whois>=0.9.0
dnspython>=2.6.0

# ─── Memory & RAG ────────────────────────────────────────────────────────────
# chromadb>=0.4.0                 # Vector DB (semantic memory)
# sentence-transformers>=2.7.0   # Embeddings for RAG

# ─── Security (SOC tools) ────────────────────────────────────────────────────
# yara-python>=4.5.0             # YARA rule scanning

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
