# VAL v13.0 — Unified Dependencies
# ===================================
# STEP 1 — Install PyTorch (choose your build):
#
#   GPU (CUDA 12.1):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
#
#   GPU (CUDA 12.4+):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu124
#
#   CPU only (slow inference, working):
#     pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cpu
#
# STEP 2 — Install all other deps:
#     pip install -r requirements.txt

# ─── Core Runtime ─────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
psutil>=5.9.0

# ─── ML Framework ─────────────────────────────────────────────────────────────
# torch must be installed separately (see STEP 1)
transformers>=4.40.0
accelerate>=0.28.0
sentencepiece>=0.1.99
tokenizers>=0.15.0

# ─── Quantization (GPU — enables 4-bit NF4 for Qwen/Mistral) ─────────────────
# Required for VRAM-efficient inference with 7B models
bitsandbytes>=0.43.0

# ─── API Server ───────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# ─── OSINT (optional, improves results) ───────────────────────────────────────
python-whois>=0.9.0
dnspython>=2.6.0

# ─── Voice (optional) ─────────────────────────────────────────────────────────
# STT — Whisper (fast for en):
# openai-whisper>=20231117
#
# TTS — pyttsx3 (system TTS, zero deps):
# pyttsx3>=2.90

# ─── Wikipedia + RAG (optional, enables wiki_search + semantic retrieval) ─────
# wikipedia-api>=0.7.1
# sentence-transformers>=2.7.0
# faiss-cpu>=1.8.0

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest>=8.0.0
pytest-cov>=5.0.0
